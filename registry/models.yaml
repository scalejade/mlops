# Model registry — source of truth for every model we host on the Hub.
# One entry per repo under huggingface.co/scalejade.
# Weights live on the Hub, never in git. This file records provenance.

models:
  - name: qwen-sea-lion-v4-32b-it
    hub_repo: scalejade/qwen-sea-lion-v4-32b-it
    upstream: aisingapore/Qwen-SEA-LION-v4-32B-IT
    relation: redistribution        # redistribution | finetune | merge | quantization
    params: 32B
    context_len: 40960
    license: MIT
    status: production              # production | candidate | deprecated | failed
    notes: "Selected for PJP clause extraction. 1x RTX PRO 6000 96GB."

  - name: qwen-sea-lion-v4.5-27b-it
    hub_repo: scalejade/qwen-sea-lion-v4.5-27b-it
    revision: 81d9102bab84b46085cc0f8539efe578d33e29da
    # 2026-08-20 this mirror was wrong: 20 shards / 80.15 GB of Qwen3ForCausalLM @
    # 40960, i.e. not v4.5 at all — most likely clone_model.py re-run with a different
    # SOURCE_MODEL against the same TARGET_MODEL in .env.
    # 2026-08-25, re-checked against the Hub API: it is now 15 shards / 55.56 GB of
    # Qwen3_5ForConditionalGeneration @ 262144, matching upstream shard-for-shard.
    # The mirror was re-cloned on 2026-08-20T15:23Z. BROKEN cleared, sha pinned.
    upstream: aisingapore/Qwen-SEA-LION-v4.5-27B-IT
    relation: redistribution
    params: 27B
    context_len: 262144             # config.json text_config.max_position_embeddings
    architecture: Qwen3_5ForConditionalGeneration   # hybrid linear attention + vision tower
    license: MIT
    status: candidate
    notes: >-
      Fine-tuning base as of 2026-08-25 (training/configs/example-lora.yaml).
      Needs transformers >=5.2.0 and bf16; unusable on the RunPod stock vLLM
      worker, which is why it is candidate and not production.

  - name: gemma-4-31B-it
    hub_repo: scalejade/gemma-4-31b-it-baseline
    upstream: google/gemma-4-31B-it-qat-w4a16-ct
    relation: redistribution
    params: 31B
    context_len: null
    license: gemma
    status: failed
    notes: "Architecture unsupported by vLLM 0.27.1 (RunPod template v2.25.1). Revisit on newer vLLM."
