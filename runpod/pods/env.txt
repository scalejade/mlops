# RunPod pod environment variables. Paste into Edit Pod -> Environment variables,
# or use the "Raw editor". One KEY=VALUE per line.
#
# HF_TOKEN must go in via the key icon as a Secret, not as plaintext.

HF_TOKEN=<use the key icon -> Secret>
HF_HOME=/workspace/.huggingface
HF_XET_HIGH_PERFORMANCE=1
VLLM_CACHE_ROOT=/workspace/.vllm-cache
VLLM_API_KEY=sk-jago-ww8ic2q2mby6pj2miuq3vjeb
PYTORCH_ALLOC_CONF=expandable_segments:True
PUBLIC_KEY=<your ssh public key, or "null" for no SSH>

# Optional, ONLY if the image has the hf_transfer package installed.
# Setting this without the package makes every download fail with ImportError.
#   HF_HUB_ENABLE_HF_TRANSFER=1
