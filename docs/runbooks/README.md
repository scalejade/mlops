# Runbooks

Step-by-step procedures for things done under time pressure, written so someone
who did not build the system can follow them.

Candidates: deploying a new endpoint, rolling back a bad model, rotating HF and
RunPod credentials, responding to a truncated-output report, recovering a worker
stuck in `throttled`.
